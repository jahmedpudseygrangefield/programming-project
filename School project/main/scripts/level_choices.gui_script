--INIT/RELEASE SCRIPT
function init(self)
	msg.post(".", "acquire_input_focus")
end
function final(self)
	msg.post(".", "release_input_focus")
end

--This is v1 test 
function on_input(self, action_id, action)
	if action.pressed and gui.pick_node(gui.get_node("TEST"), action.x, action.y) then 
		msg.post("main:/controller", "TEST")
	end
end
